#ifndef checkCollision_hpp
#define checkCollision_hpp

// This code is only useful for non-physics box colliders.

/*
//Project Files
#include "object.hpp"

//Pragma
#pragma once

// Check collision for AABBs.
bool Check_Collision(Object& one, Object& two)
{
    if (one.collider.Position.x + one.collider.Scale.x >= two.collider.Position.x - two.collider.Scale.x && two.collider.Position.x + two.collider.Scale.x >= one.collider.Position.x)
    {
        if (one.collider.Position.y + one.collider.Scale.y >= two.collider.Position.y - two.collider.Scale.x && two.collider.Position.y + two.collider.Scale.y >= one.collider.Position.y)
        {
            if (one.collider.Position.z + one.collider.Scale.z >= two.collider.Position.z - two.collider.Scale.z && two.collider.Position.z + two.collider.Scale.z >= one.collider.Position.z)
            {
                return true;
            }
        }
    }

    return false;
}
*/

#endif
